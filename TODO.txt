IMPROVE bettercap options launch
  -logging
  -better password harvesting
IMPROVE credentials filter
  -where from [IP]
  -where from [site]
  -exactly only login and password(without unnecessary informations)
TRY with HSTS hijack from Jakub
ADD connecting to protected WLAN
